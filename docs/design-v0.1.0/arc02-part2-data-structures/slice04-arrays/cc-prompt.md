# CC Prompt — Slice 04 (Arrays)

Publish the Arrays chapter multi-leaf. Source:
`workbench/arrays/new-section-arrays.md`. Target: `src/part2/arrays/`. Apply
arc01 §6 + arc02 §A2. Work the ledger; slug→section map in `slice-doc.md`.

Dir is README-only (confirmed by Read-probe) — you CREATE the 13 leaf files.
README: Read the `# Arrays` placeholder then overwrite with the draft H1 + the
two intro paragraphs + the "## A Brief Philosophical Interlude" section.

- Each `##` → its slug leaf, full heading as both `#` title and SUMMARY text.
- `lisp`→`lfe` fences. Draft uses `lfe>` REPL prompts — keep.
- The draft's **bold** inline sub-labels (`**Setting values:**`, `**Mapping:**`,
  etc.) are NOT headings — keep them as bold text verbatim.
- `pragmatism.md` keeps the closing "...as reasonable a choice as any." line.
- `SUMMARY.md`: replace `  * [Arrays](part2/arrays/README.md)` with that line +
  13 nested entries at one deeper indent.

Don't touch other chapters / SUMMARY blocks. Preserve prose/code verbatim.
Finish with `closing-report.md` + hand off for `cdc-verification.md`.
